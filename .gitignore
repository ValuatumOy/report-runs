# Everything the dashboard is built from is tracked, including the PDFs and
# snapshots, so a cloud agent can grade and rebuild from a clone alone.
# .vercelignore is what keeps the snapshots and logs off the public site.
index.html
rubric.html
logs/
.vercel/
.DS_Store
