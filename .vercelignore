# Kept out of the deployed site: raw run data, logs and the launchd wrapper.
# build.mjs must stay — the Vercel build runs it to generate index.html.
runs/**/snapshot.json
runs/**/run.log
nightly.sh
logs/
